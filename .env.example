# PostgreSQL
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/concert_reservation"

# JWT — 생성: openssl rand -base64 64
JWT_SECRET="your-super-secret-jwt-key"
JWT_EXPIRES_IN="7d"

# 포트
SERVER_PORT=4000

# Frontend URL (CORS)
CLIENT_URL="http://localhost:3000"

# Frontend에서 backend 호출 시
API_URL="http://localhost:4000"
